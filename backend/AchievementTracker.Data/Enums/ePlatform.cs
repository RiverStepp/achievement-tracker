namespace AchievementTracker.Data.Enums;

// CREATE A NEW EF MIGRATION IF YOU EVER UPDATE THIS ENUM!
// There is a constraint that depends on it 
public enum ePlatform: byte
{
    Windows = 1,
    Mac = 2,
    Linux = 3
}

